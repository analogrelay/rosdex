http:
  host: "0.0.0.0"
  cors:
    enabled: true
    allow-origin: "*"
    allow-headers: "Authorization, X-Requested-With, Content-Type, Content-Length"
    allow-credentials: true

transport:
  host: "127.0.0.1"